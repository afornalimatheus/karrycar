meta {
  name: update
  type: http
  seq: 3
}

patch {
  url: {{domain}}/provider/{{provider}}/vehicles/:vehicleId
  body: json
  auth: inherit
}

params:path {
  vehicleId: 11
}

body:json {
  {
    "model": "250-D"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
